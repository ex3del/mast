# <Имя проекта>

<Одна-две строки: что это за проект и для кого.>

## Куда смотреть

| Документ | Когда читать |
|---|---|
| [ROADMAP.md](ROADMAP.md) | что осталось сделать |

## Стек

- <язык, фреймворк, база данных — то, на чём стоит проект>

## Команды

```bash
<команда тестов>
<команда линта или сборки>
```

## Код

- **Неожиданное состояние — падай громко или верни явный `unknown`.** Пустой `except`,
  дефолт вместо ошибки, `return None` вместо проверки — программа продолжает работать
  на неверных данных, и сбой всплывает далеко от причины.

## Инварианты проекта

- <правило, которое нельзя нарушать молча>

**Держать этот файл до 200 строк.** Справочник — архитектура, схемы, карточки — в `docs/`;
договорённости по конкретной папке — в `.claude/rules/*.md` с `paths:`.
