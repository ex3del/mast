# TODO

Всякое, что накопилось и не забыть. Не приоритизировано, просто список.

- Сделать экспорт отчётов в PDF без ограничения на 500 задач — сейчас воркер падает
  по таймауту Celery на больших проектах, люди жалуются.
- Добавить рефреш-токены, а то все злятся, что разлогинивает раз в сутки.
- Разобраться, почему `test_login_rate_limit` иногда падает в CI — похоже, общий Redis
  между прогонами, но руки не дошли проверить.
- Проверить `assignee_id` на принадлежность к проекту при назначении задачи — сейчас можно
  назначить человека из другой команды, это баг.
- Обновить SQLAlchemy до 2.x (кажется, уже обновили? надо перепроверить requirements.txt).
- Написать миграцию для индекса по `tasks.due_date` — сейчас фильтр по просроченным
  задачам делает full scan на больших проектах.
- Подумать над стейджингом, а то тестируем прямо на анонимизированном дампе прода, это
  не очень масштабируется.
- Перевести фронтенд-репозиторий на новую версию API-клиента (не наш репозиторий, но
  трекать здесь, чтобы не забыть согласовать даты релиза).
- Добавить rate limiting не только на логин, но и на создание задач — кто-то один раз
  накрутил скриптом 10000 задач в тестовом проекте и уронил базу.
- Сходить к безопасникам по поводу JWT_SECRET — он один на все окружения, надо разное
  на прод и стейджинг, когда стейджинг появится.
