"""Имя плагина по языку: оно входит в ссылки на команды и скиллы, которые печатают хуки.

Источник один на оба хука, потому что расходятся они молча: при переходе на два
плагина имена в `plugin.json` поменяли, а литералы в `core.py` и `roadmap_lint.py`
остались от старой схемы — пользователь получал ссылку на несуществующую команду.
Сторож `tests/test_plugin_manifest.py` сверяет этот словарь с манифестами.
"""
PLUGIN = {"ru": "mast-ru", "en": "mast"}
