# Закрытые пункты

По два тезиса на пункт: что изменилось с числом, дата, диапазон коммитов, ссылки вниз.
Снятые — подразделом «Снято», строкой с причиной. Первый слой поиска «а это уже делали?»
и «а это уже пробовали и отказались?».

## A. Плагин

- **A-11** Диспетчер ставит контракт, а не план; вопрос человеку не останавливает очередь — 23.09 · `1aa866a..3975f63` · [ресёрч](../research/2026-09-23-dispatcher-load.md)
  План и `STATUS.md` заводит только сессия пункта, контекст разговора с человеком — в строку пункта, соседи по путям договариваются напрямую. Вопрос человеку — текстом и слотом `ждёт человека: <вопрос>`: `roadmap_lint.py --waiting` на фикстуре 2 из 2, лишних 0, слот без вопроса — ошибка 1 из 1; указаний диспетчеру заводить `STATUS.md` в «Запуск пункта» 2 → 0 на язык, образец промпта старта 137/157 символов. Сторожей 111, версия 3.3.1.

- **A-9** `/init-project`: предложения блоком `diff`, вопрос об удалении оригинала после переноса — 22.09 · `e1de610..521d223`
  На песочнице из фикстуры `legacy`: вопрос «удалить ли оригинал» после переноса `TODO.md` и `CHANGELOG.md` — 0 из 2 прогонов → 2 из 2; блоков `diff` в шаге 2 — 0 и 1 → 11 и 9, каждый существующий файл показан диффом. Проверено через `--check`, где команда перечисляет вопросы, но не задаёт их. Сторожей 96, версия 3.2.0.

- **A-1** Плагин MAST: ядро в `SessionStart`, процедуры скиллами, команда `/init-project` — 21.09 · `9c22062..ca80e08` (9c22062 — корневой коммит, пункт занял всю историю репозитория) · [STATUS](done/A-1/STATUS.md)
  Метод едет плагином и не трогает `~/.claude/CLAUDE.md`: ядро 4596 (ru) и 5029 (en) символов при лимите площадки 10 000, три скилла в двух языках, `/init-project` разворачивает 4 файла каркаса за прогон, 53 сторожа зелёные.

- **A-2** Правила из разбора Ouroboros: триггер «проза → guard», SSOT, fail loud, защищённый путь, архив снятых пунктов — 21.09 · `c2d94eb..2d617fe`
  5 правил доехали в обе локали (11 файлов), линт научился различать снятые пункты в архиве: зависимость на снятый больше не пускает пункт в очередь готовых. 55 сторожей, версия плагина 1.1.1.

- **A-3** Дефекты финального ревью: fail loud в хуках, линт архива, устаревшие после A-2 тексты — 21.09 · `a1bc00f..2ce6484`
  Закрыто 7 дефектов, найденных тремя параллельными ревью: хук ядра больше не падает на битой кодировке и удалённом cwd, `pre-commit` без `python3` отказывает вместо молчаливого пропуска, `DONE.md` проходит линт наравне с роадмапом, заголовок «Снято» узнаётся с хвостом и эмодзи, вызов `superpowers` получил оговорку. Сторожей 55 → 61, версия плагина 1.2.0, заведён `TECH_DEBT.md` (3 долга).

- **A-4** `/init-project`: анализ кодовой базы и требование superpowers — 21.09 · `9c0a215..535f2ba`
  Команда вернула себе анализ репозитория: на живом прогоне (проект на TypeScript с Vitest, Docker и GitHub Actions) заполнила в `CLAUDE.md` стек, 5 команд, тесты, инфраструктуру, структуру и конвенции реальными данными — 0 придуманных фактов, 1 плейсхолдер остался там, где данных в репозитории не бывает. Зависимость от `superpowers@claude-plugins-official` объявлена в манифесте и разрешена в маркетплейсе, минимум Claude Code поднят до 2.1.242. Сторожей 62, версия плагина 1.3.0.

- **A-5** Команда `/init-project` в двух языках — 21.09 · `cbb8e06..6977ea6`
  Команд стало две: `/mast:init-project` (269 строк, английская) и `/mast:init-project-ru` (266). Язык больше не угадывается по тексту хука в контексте — его задаёт выбор команды, поэтому шаг определения языка ушёл из обеих версий. Сторож сверяет паритет: равное число разделов и ограждений (5), 0 кириллицы в английской, каждая версия ссылается только на свою локаль. На живом прогоне английская развернула английский каркас: 0 кириллицы в созданных файлах, стек заполнен реальными данными. Сторожей 65, версия плагина 1.4.0.


- **A-8** Настройка `always_core`: ядро и в неразвёрнутом проекте — 22.09 · `88b37e6..a3487f2`
  Ядро приезжало только туда, где уже есть `ROADMAP.md` или `.claude/rules/` — для постороннего это правильно, а для того, кто перевёл на плагин свой глобальный `CLAUDE.md`, означало потерю правил везде, где каркаса нет. Теперь это выбор: `userConfig.always_core`, по умолчанию выключено. Подстановка `${user_config.always_core}` в `env` разводки хука **не сработала** на живом прогоне 3.1.0, хотя документация её обещает; работает автоматическая переменная `CLAUDE_PLUGIN_OPTION_ALWAYS_CORE`, поэтому хук читает обе. Приёмка на установленном плагине: с настройкой в пустом проекте пришли подсказка и ядро (маркер найден, 11 790 символов), без неё — одна подсказка в 133 символа; в развёрнутом проекте настройка ничего не меняет. `--config` применяется только при настоящей установке, у уже установленного плагина `install` молча ничего не пишет — это в обоих README вместе со способом правки `settings.json` руками. Сторожей 92, версия 3.1.1.

- **A-7** Плагин самодостаточен: код и тексты внутри `plugins/<язык>/` — 22.09 · `a5d2941..6dd74b2`
  Установка из маркетплейса вскрыла, что площадка копирует в кэш только содержимое каталога плагина: `${CLAUDE_PLUGIN_ROOT}` = `~/.claude/plugins/cache/ex3del/mast-ru/<версия>`, и путь `../../hooks/core.py` из A-6 вёл в пустоту — на 2.5.0 ядро не вложилось, тела скиллов не читались, линт не запускался (A-6 проверялся через `--plugin-dir`, где корнем был весь репозиторий, поэтому проверка и прошла). Источник остался один — корневые `hooks/` и `locales/`, копии внутрь плагинов раскладывает `tools/sync_plugins.py`, отставшую копию не пропускают ни pre-commit, ни сторож. Приёмка на установленном из маркетплейса 3.0.0: маркер ядра в контексте найден, 3 скилла из 3 прочитали тела, линт вернул код 2 со ссылкой на `mast-ru:managing-roadmap-items`, 5 шаблонов на месте, `/mast-ru:init-project` в списке команд. Сторожей 85, версия 3.0.0.

- **A-6** Два плагина вместо языковой настройки: `mast` и `mast-ru` — 22.09 · `caf199c..bc304df`
  Из одного репозитория собираются два плагина: `plugins/en` → `mast`, `plugins/ru` → `mast-ru`. У пользователя стало 3 скилла без суффикса `-ru` и 1 команда вместо 6 и 2, а `userConfig.language` исчез — язык это выбор при установке. В подкаталогах плагинов только обёртка (манифест, разводка хуков, скиллы-указатели, команда, `.mcp.json`), код и тексты общие в корне: `${CLAUDE_PLUGIN_ROOT}/../../hooks/core.py` проверен живьём, язык ядру приходит аргументом из разводки своего плагина. Оба прогнаны через `--plugin-dir` — каждый вложил ядро своего языка. Сторожей 74, версия 2.0.0.

## B. Продвижение

- **B-2** Приём вкладов: CONTRIBUTING, CI, защита `main`, шаблоны — 22.09 · `7e85643..4c27e52`
  Вклад теперь идёт через PR: CI на каждый PR и push в `main` гоняет тесты, сверяет копии плагина и бамп версии, `main` без зелёного `test` PR не принимает, ветки удаляются после мерджа. Проверка PR #1 без бампа — CI красный, мердж заблокирован, 1 из 1; community health 42% → 85% (CONTRIBUTING на двух языках, шаблоны issue и PR, кодекс поведения — YAML-формы issue GitHub в профиле не засчитывает). Закрыт долг «бамп версии защищён только локальным `git commit`». Сторожей 104, версия плагина не менялась. Журнал — [done/B-2/STATUS.md](done/B-2/STATUS.md).
