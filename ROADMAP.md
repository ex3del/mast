# ROADMAP

Разделы буквами, пункты цифрами внутри раздела. Держит только открытое; закрытое и снятое
уезжают в [docs/roadmap/DONE.md](docs/roadmap/DONE.md).

## A. Плагин

- **A-3** Дефекты финального ревью: fail loud в хуках, линт архива, устаревшие после A-2 тексты — 🔨 в работе · основная копия · сессия `claude-dispatch` · с 21.09
  Мои пути: `hooks/**`, `locales/**`, `tests/**`, `.githooks/**`, `README*.md`, `docs/roadmap/A-3/**`
  Готово когда: `core.py` не падает ни на битой кодировке ядра, ни на удалённом cwd — 2 теста; `pre-commit` без `python3` отклоняет коммит, а не пропускает; `DONE.md` проходит линт наравне с `ROADMAP.md`, заголовок «Снято» распознаётся с суффиксом; ни один текст не обещает `superpowers` без оговорки; тестов ≥ 60, все зелёные; версия в `plugin.json` поднята.

- **A-4** `/init-project`: анализ кодовой базы и требование superpowers — запланирован · —
  Зависит от: A-3
  Мои пути: `commands/**`, `locales/*/templates/**`, `tests/**`, `docs/roadmap/A-4/**`
  Готово когда: команда на живом чужом проекте заполняет в `CLAUDE.md` стек, команды тестов и конвенции реальными данными из репозитория — 0 плейсхолдеров `<...>` в тех секциях, где данные найдены, и ни одного придуманного факта в тех, где не найдены; требование `superpowers` объявлено там, где площадка это позволяет.
