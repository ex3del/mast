# ROADMAP

Разделы буквами, пункты цифрами внутри раздела. Держит только открытое; закрытое и снятое
уезжают в [docs/roadmap/DONE.md](docs/roadmap/DONE.md).

## A. Плагин

- **A-9** `/init-project`: вопрос об оригинале после переноса и настоящие диффы — 🔨 в работе · `основная копия` · сессия `claude-dispatch` · с 22.09
  Ручная проверка 22.09 на проекте с историей: после переноса `TODO.md` → `ROADMAP.md` и `CHANGELOG.md` → `DONE.md` оригиналы остаются рядом — две истины об одном, и агент говорит об этом только в итоге. Предложения по существующим файлам показаны текстом целиком (одно сообщение шага 2 — 18 тыс. символов без единого блока `diff`), хотя ограждение 3 обещает «было → станет».
  Мои пути: plugins/*/commands/init-project.md, tests/test_init_check.py
  Готово когда: на песочнице из `tests/fixtures/legacy/` в 2 прогонах из 2 команда в шаге 2 спрашивает, удалить ли оригинал каждого перенесённого файла, и каждое предложение по существующему файлу показывает блоком `diff`; сторож в `tests/test_init_check.py` требует оба пункта в тексте обеих локалей.

## B. Продвижение

- **B-1** Демо-GIF в шапке README — запланирован · —
  Плагин без картинки работы не ставят: README объясняет метод словами, но не показывает. Нужна запись одного терминала, где диспетчер ведёт несколько пунктов.
  Мои пути: assets/**, README.md, README.ru.md
  Готово когда: GIF ≤ 30 с и ≤ 5 МБ стоит в шапке обоих README и показывает в одном терминале, как диспетчер запускает ≥ 2 пункта параллельно, вливает ветку и переносит закрытый пункт в `DONE.md`.
