# ROADMAP

Разделы буквами, пункты цифрами внутри раздела. Держит только открытое; закрытое и снятое
уезжают в [docs/roadmap/DONE.md](docs/roadmap/DONE.md).

## A. Плагин

- **A-6** Два плагина вместо языковой настройки: `mast` и `mast-ru` — 🔨 в работе · основная копия · сессия `claude-dispatch` · с 22.09
  Мои пути: `.claude-plugin/**`, `plugins/**`, `hooks/**`, `skills/**`, `commands/**`, `locales/**`, `tests/**`, `README*.md`, `docs/roadmap/A-6/**`
  Готово когда: маркетплейс отдаёт два плагина, каждый ставится отдельно; у пользователя 3 скилла без суффикса `-ru` и 1 команда вместо 6 и 2; общий код хуков и тексты локалей не продублированы — в `plugins/*/` только манифест, разводка хуков, скиллы-указатели, команда и `.mcp.json`; `userConfig.language` удалён; живой прогон каждого плагина через `--plugin-dir` вкладывает ядро своего языка; сторож локалей — 0 расхождений; тестов ≥ 68, все зелёные.
