# ROADMAP

Разделы буквами, пункты цифрами внутри раздела. Держит только открытое; закрытое уезжает
в [docs/roadmap/DONE.md](docs/roadmap/DONE.md).

## A. Плагин

- **A-1** Плагин MAST: ядро в `SessionStart`, процедуры скиллами, команда `/init-project` — 🔨 в работе · основная копия · сессия `claude-dispatch` · с 20.09
  Мои пути: `.claude-plugin/**`, `hooks/**`, `skills/**`, `commands/**`, `locales/**`, `tests/**`, `docs/roadmap/A-1/**`
  Готово когда: плагин ставится на чистой машине двумя командами и не меняет ни байта в `~/.claude/CLAUDE.md`, а в `~/.claude/settings.json` появляется только штатная запись о включении плагина; текст ядра ≤ 6000 символов при лимите 10 000; `/init-project` в пустом каталоге создаёт 4 файла каркаса за один прогон, а на файл, который площадка не даёт записать, выдаёт человеку готовую команду; `/init-project --check` на чужом проекте меняет 0 файлов; сторож локалей — 0 расхождений в структуре заголовков `ru` и `en`.
