{
  "name": "mast-ru",
  "version": "3.3.1",
  "description": "Method for Agents, Sessions and Tasks — пункты роадмапа, worktree на пункт, диспетчер, правила с paths:",
  "license": "MIT",
  "dependencies": [
    {
      "name": "superpowers",
      "marketplace": "claude-plugins-official"
    }
  ],
  "userConfig": {
    "context7_key": {
      "type": "string",
      "title": "Context7 API key (optional)",
      "description": "Optional key for the bundled Context7 MCP server; the free tier works without it",
      "sensitive": true
    },
    "always_core": {
      "type": "boolean",
      "title": "Ядро метода во всех проектах",
      "description": "Вкладывать ядро и там, где каркас метода ещё не развёрнут (нет ROADMAP.md и .claude/rules/). По умолчанию в таких проектах приходит только строка-подсказка.",
      "default": false
    }
  }
}
